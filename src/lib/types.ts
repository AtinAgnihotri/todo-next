export enum Status {
  BACKLOG = "backlog",
  TODO = "todo",
  IN_PROGRESS = "in progress",
  DONE = "done",
  CANCELLED = "canceled",
}

export enum Priority {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}
